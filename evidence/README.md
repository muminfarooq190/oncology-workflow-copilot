# Evidence corpus

Evidence is ingested from an allowlisted, public or openly licensed source set and frozen per corpus version. Do not commit paid guideline content.

The initial `nsclc-v1` manifest is intentionally empty during Week 5. Week 8 will add ingestion, chunk hashes, hybrid retrieval, and gold relevant-chunk labels. Evaluation runs must fail if the configured corpus version differs from the case manifest.

